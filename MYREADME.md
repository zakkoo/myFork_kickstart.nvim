# Installation

## On Mac you need following tools


```bash
brew install ripgrep fd
```

# Shortkeys Notes

## Default

ciw = change inner word
ci" = change inner ""
yyp = ctrl c, ctrl v
$ = jump to end of line
G = end of file
V % y = go to the line with the bracket like {} and this will copy all of it inside it the brackets


## csharp lsp
